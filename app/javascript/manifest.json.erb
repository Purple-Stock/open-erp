<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Purchase Purple Stock",
  "short_name": "Purple Stock",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "https://purple-stock.s3-sa-east-1.amazonaws.com/purple-stock.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}
